<%= form_for(@anecdote) do |f| %>
  <% if @anecdote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@anecdote.errors.count, "error") %> prohibited this anecdote from being saved:</h2>

      <ul>
      <% @anecdote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :subject %>
    <%= f.text_field :subject %>
  </div><br>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div><br>

  <%= f.select :chapter_id, @chapters.map{ |c| [c.title, c.id] }  %>

  <br/>
  <%= f.submit "Create anecdote", class: "btn btn-large btn-primary" %>
<% end %>