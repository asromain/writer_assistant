<div class="contenu">
	<% if (@chapters.blank?) then %>
	  	<h2>No chapter for now.</h2>
	<% else %>
		<h2>List of exisiting chapters.</h2>
		<ul>  
			<%- @chapters.each do |chapter| %>
			  	<li>N°<%= chapter.id %> : <%= chapter.title %></li>
			<%- end %>
		</ul>
	<% end %>
</div>